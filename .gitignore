node_modules
dist
coverage
playwright-report
test-results
src-tauri/target
.vite
.DS_Store
.test-dist/

# Per-machine Pi config (each machine maintains its own).
# Canonical statusline source lives at .pi/install/ and is replicated to
# ~/.pi/agent/extensions/ on each machine via the install README.
.pi/settings.json

# Dispatch-auto orchestrator runtime files (per-machine, not synced).
.dispatch-auto.log
.dispatch-auto-*.log
agents/channels/.sessions/

# Dispatch-auto per-machine config overrides (Claude account paths, Pi profile
# routing, etc.). The committed orchestration.json is the shared baseline; this
# file deep-merges over it. Each machine maintains its own.
agents/orchestration.local.json