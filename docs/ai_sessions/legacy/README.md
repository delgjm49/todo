# Legacy AI Session Artifacts

These files belong to the previous 3-agent (Main / Dev / Review) checkpoint workflow used through Epics 07–08 (checkpoints 6–12).

They are kept here for historical reference. The active workflow now lives under `agents/` at the repo root and uses a 4-agent flow (Main → Plan → Dev → Review) with append-only dispatch channels.

See:

- [`AGENTS.md`](../../../AGENTS.md) for the project pointer doc
- [`agents/README.md`](../../../agents/README.md) for the active workflow
- [`docs/SESSIONS.md`](../../SESSIONS.md) for the current session log (replaces the old `HANDOFF.md` / `STATUS.md` / `WORKLOG.md` split)

Do not append new entries here. Do not create new files in this folder.
